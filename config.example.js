// Example configuration for FactFusion
// Copy this file to config.js and replace the placeholder API key
const config = {
    API_KEY: 'your_huggingface_api_key_here'
};

// Export the configuration
export default config;
